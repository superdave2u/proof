# specs/

One file per bounded area, authored in planning loops, consumed every build loop.

- `cards/` — one file per suit/category, 10 cards each, 2 wild cards, together exactly 52 unique cards.
- Cards follow the anatomy defined in `../SPEC.md`.

Rules for spec authors (Ralph):

- Search before creating — never assume a spec is missing or exists.
- No contradictions between files; SPEC.md wins ties.
- Card content is substantive and final — no TODOs, no lorem ipsum.
